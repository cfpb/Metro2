version: '3.4'

volumes:
  postgres_data:
  evaluators:

services:
  postgres:
    image: postgres:13-alpine
    user: postgres
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_PASSWORD: cfpb
      POSTGRES_DB: metro2
    ports:
      - 127.0.0.1:5432:5432
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready" ]
      interval: 3s
      timeout: 3s
      retries: 10
    shm_size: '256mb'
  evaluator:
    image: metro2_evaluator:local
    build:
      context: .
      dockerfile: Dockerfile
      shm_size: '256mb'
    shm_size: '256mb'
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./metro2:/src/metro2/metro2
    stdin_open: true
    tty: true
    env_file:
      - .env
    environment:
      PGDATABASE: metro2
      PGUSER: postgres
      PGPASSWORD: cfpb
      POSTGRES_USER: postgres
      PGHOST: postgres
      PGPORT: 5432
      METRO2ENV: local
